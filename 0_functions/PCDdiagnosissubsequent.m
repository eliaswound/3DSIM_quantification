%clear;close all;clc;
folder_name=uigetdir('Please select the folder that contains all the .mat file generated by PCD_diagnosis_v4_freedraw_contrast_3D');
cd(folder_name);
files = dir([folder_name '\*_requireddata.mat']);


if isempty(files)
    display('no files identified under the current folder')
end

all=[];
for i=1:length(files)
    data=importdata(files(i).name);
    all=[all; double(data.threshold(1)),...
    double(data.threshold(2)),...
    double(data.colocalization_GreenOverlapIntIntensityRatio),...
    double(data.cell_volume),...
    double(data.DNAH11_integratedintensity),... 
    double(data.tubulin_integratedintensity),...
    double(data.DNAH11_cilia_integratedintensity),... 
    double(data.colocalization_RedOverlapArea),...
    double(data.colocalization_RedOverlapIntIntensityRatio)];
end

% all structure
%     column1: data.threshold(1),...
%     column2: data.threshold(2),...
%     column3: data.colocalization_GreenOverlapIntIntensityRatio,...
%     column4: data.cell_volume,...
%     column5: data.DNAH11_integratedintensity,... 
%     column6: data.tubulin_integratedintensity,...
%     column7: data.DNAH11_cilia_integratedintensity,... 
%     column8: data.colocalization_RedOverlapArea,...
%     column9: data.colocalization_RedOverlapIntIntensityRatio]